# Copy to `.env`. Only set the key(s) for the provider(s) you use.
EURI_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=
ANTHROPIC_API_KEY=
# Amazon Bedrock — uses the standard AWS credential chain (no API key here).
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
